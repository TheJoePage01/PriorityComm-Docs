[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "PriorityComm"
authors = [{name = "TheJoePage", email = "joseph.a.pc@proton.me"}]
dynamic = ["version", "description"]
